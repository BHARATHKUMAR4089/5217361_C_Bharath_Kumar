long sumXor(long n) {
    if (n == 0) {
        return 1; 
    }

    int zero_bits = 0;
    long temp = n;

    while (temp > 0) {
        if ((temp & 1) == 0) {
            zero_bits++;
        }
        temp >>= 1;
    }

    return 1L << zero_bits;  
}